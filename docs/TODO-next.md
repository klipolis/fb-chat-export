# TODO Next

No active development tasks are pending at this time.

Use this file to capture the next work items once the CI workflow and build pipeline are stabilized.

- Example next item: add a GitHub Action test step for generated `dist/userscript.js` contents.
- Example next item: add a second workflow for release or artifact publishing.
