# Tests

This directory is reserved for automated tests, fixtures, and validation scripts.

- Put unit tests for shared parsing, rule matching, and export formatting here.
- Put integration test fixtures for generated `dist/userscript.js` output here.
- Keep tests separate from the main `src/` code and documentation.
